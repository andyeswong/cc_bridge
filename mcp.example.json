{
  "_comment": "Copy to a host-only file (e.g. mcp.json) pointed to by MCP_CONFIG_PATH. NEVER commit the real one — it holds credentials. Same shape as Claude Code's .mcp.json.",
  "mcpServers": {
    "example-stdio-server": {
      "command": "npx",
      "args": ["-y", "some-mcp-server"],
      "env": {
        "API_KEY": "REPLACE_ME"
      }
    },
    "example-http-server": {
      "type": "http",
      "url": "https://mcp.example.com",
      "headers": {
        "Authorization": "Bearer REPLACE_ME"
      }
    }
  }
}
