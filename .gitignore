# Windows build + runtime artifacts (the Linux `claude-bridge` binary stays tracked)
*.exe
usage.db
bridge.log

# Local MCP server registry — holds credentials, keep out of this public repo.
# Commit only mcp.example.json (placeholders).
mcp.json
mcp.*.json
!mcp.example.json
